### YamlMime:UniversalReference
items:
  - uid: office.OfficeExtension.TrackedObjects
    summary: '<span data-ttu-id="45403-101">Collection d''objets suivis, contenue dans un contexte de requête.</span><span class="sxs-lookup"><span data-stu-id="45403-101">Collection of tracked objects, contained within a request context.</span></span> <span data-ttu-id="45403-102">Pour plus d''informations, voir «Context. trackedObjects».</span><span class="sxs-lookup"><span data-stu-id="45403-102">See "context.trackedObjects" for more information.</span></span>'
    name: OfficeExtension.TrackedObjects
    fullName: OfficeExtension.TrackedObjects
    langs:
      - typeScript
    type: class
    package: office
    children:
      - office.OfficeExtension.TrackedObjects.add
      - office.OfficeExtension.TrackedObjects.add_1
      - office.OfficeExtension.TrackedObjects.remove
      - office.OfficeExtension.TrackedObjects.remove_1
  - uid: office.OfficeExtension.TrackedObjects.add
    summary: |-
      <span data-ttu-id="45403-103">Effectuer le suivi d'un nouvel objet pour un ajustement automatique en fonction des modifications apportées au document.</span><span class="sxs-lookup"><span data-stu-id="45403-103">Track a new object for automatic adjustment based on surrounding changes in the document.</span></span> <span data-ttu-id="45403-104">Seuls certains types d'objets en ont besoin.</span><span class="sxs-lookup"><span data-stu-id="45403-104">Only some object types require this.</span></span> <span data-ttu-id="45403-105">Si vous utilisez un objet sur les appels «. Sync» et en dehors de l'exécution séquentielle d'un lot «. Run» et que vous obtenez une erreur «InvalidObjectPath» lors de la définition d'une propriété ou de l'appel d'une méthode sur l'objet, vous deviez ajouter l'objet à la collection d'objets suivis. lors de la création initiale de l'objet.</span><span class="sxs-lookup"><span data-stu-id="45403-105">If you are using an object across ".sync" calls and outside the sequential execution of a ".run" batch, and get an "InvalidObjectPath" error when setting a property or invoking a method on the object, you needed to have added the object to the tracked object collection when the object was first created.</span></span>

      <span data-ttu-id="45403-106">Cette méthode a également la signature suivante:</span><span class="sxs-lookup"><span data-stu-id="45403-106">This method also has the following signature:</span></span>

      <span data-ttu-id="45403-107">`add(objects: ClientObject[]): void;`Où Objects est un tableau d'objets à suivre.</span><span class="sxs-lookup"><span data-stu-id="45403-107">`add(objects: ClientObject[]): void;` Where objects is an array of objects to be tracked.</span></span>
    name: add(object)
    fullName: add(object)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'add(object: ClientObject): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: object
          description: ''
          type:
            - office.OfficeExtension.ClientObject
  - uid: office.OfficeExtension.TrackedObjects.add_1
    summary: '<span data-ttu-id="45403-108">Effectuer le suivi d''un ensemble d''objets pour un ajustement automatique en fonction des modifications apportées au document.</span><span class="sxs-lookup"><span data-stu-id="45403-108">Track a set of objects for automatic adjustment based on surrounding changes in the document.</span></span> <span data-ttu-id="45403-109">Seuls certains types d''objets en ont besoin.</span><span class="sxs-lookup"><span data-stu-id="45403-109">Only some object types require this.</span></span> <span data-ttu-id="45403-110">Si vous utilisez un objet sur les appels «. Sync» et en dehors de l''exécution séquentielle d''un lot «. Run» et que vous obtenez une erreur «InvalidObjectPath» lors de la définition d''une propriété ou de l''appel d''une méthode sur l''objet, vous deviez ajouter l''objet à la collection d''objets suivis. lors de la création initiale de l''objet.</span><span class="sxs-lookup"><span data-stu-id="45403-110">If you are using an object across ".sync" calls and outside the sequential execution of a ".run" batch, and get an "InvalidObjectPath" error when setting a property or invoking a method on the object, you needed to have added the object to the tracked object collection when the object was first created.</span></span>'
    name: add(objects)
    fullName: add(objects)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'add(objects: ClientObject[]): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: objects
          description: ''
          type:
            - 'ClientObject[]'
  - uid: office.OfficeExtension.TrackedObjects.remove
    summary: |-
      <span data-ttu-id="45403-111">Libère la mémoire associée à un objet qui a été précédemment ajouté à cette collection.</span><span class="sxs-lookup"><span data-stu-id="45403-111">Release the memory associated with an object that was previously added to this collection.</span></span> <span data-ttu-id="45403-112">Vous rencontrez de nombreux objets suivies ralentit l’application hôte, donc n’oubliez pas de libérer les objets que l'on ajoute, une fois que vous avez terminé à les utiliser.</span><span class="sxs-lookup"><span data-stu-id="45403-112">Having many tracked objects slows down the host application, so please remember to free any objects you add, once you're done using them.</span></span> <span data-ttu-id="45403-113">Vous devrez appeler `context.sync()` avant que la version de mémoire ne prenne effet.</span><span class="sxs-lookup"><span data-stu-id="45403-113">You will need to call `context.sync()` before the memory release takes effect.</span></span>

      <span data-ttu-id="45403-114">Cette méthode a également la signature suivante:</span><span class="sxs-lookup"><span data-stu-id="45403-114">This method also has the following signature:</span></span>

      <span data-ttu-id="45403-115">`remove(objects: ClientObject[]): void;`Où Objects est un tableau d'objets à supprimer.</span><span class="sxs-lookup"><span data-stu-id="45403-115">`remove(objects: ClientObject[]): void;` Where objects is an array of objects to be removed.</span></span>
    name: remove(object)
    fullName: remove(object)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'remove(object: ClientObject): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: object
          description: ''
          type:
            - office.OfficeExtension.ClientObject
  - uid: office.OfficeExtension.TrackedObjects.remove_1
    summary: '<span data-ttu-id="45403-116">Libère la mémoire associée à un objet qui a été précédemment ajouté à cette collection.</span><span class="sxs-lookup"><span data-stu-id="45403-116">Release the memory associated with an object that was previously added to this collection.</span></span> <span data-ttu-id="45403-117">Vous rencontrez de nombreux objets suivies ralentit l’application hôte, donc n’oubliez pas de libérer les objets que l''on ajoute, une fois que vous avez terminé à les utiliser.</span><span class="sxs-lookup"><span data-stu-id="45403-117">Having many tracked objects slows down the host application, so please remember to free any objects you add, once you''re done using them.</span></span> <span data-ttu-id="45403-118">Vous devrez appeler `context.sync()` avant que la version de mémoire ne prenne effet.</span><span class="sxs-lookup"><span data-stu-id="45403-118">You will need to call `context.sync()` before the memory release takes effect.</span></span>'
    name: remove(objects)
    fullName: remove(objects)
    langs:
      - typeScript
    type: method
    syntax:
      content: 'remove(objects: ClientObject[]): void;'
      return:
        type:
          - void
        description: ''
      parameters:
        - id: objects
          description: ''
          type:
            - 'ClientObject[]'