### YamlMime:UniversalReference
items:
  - uid: office.Office.TableData
    summary: '<span data-ttu-id="edf18-101">Représente les données d''une table ou d''un [Office. TableBinding](xref:office.Office.TableBinding)</span><span class="sxs-lookup"><span data-stu-id="edf18-101">Represents the data in a table or an [Office.TableBinding](xref:office.Office.TableBinding)</span></span><!-- --><span data-ttu-id="edf18-102">.</span><span class="sxs-lookup"><span data-stu-id="edf18-102"></span></span>'
    name: Office.TableData
    fullName: Office.TableData
    langs:
      - typeScript
    type: class
    package: office
    children:
      - office.Office.TableData.(constructor)
      - office.Office.TableData.(constructor)_1
      - office.Office.TableData.headers
      - office.Office.TableData.rows
  - uid: office.Office.TableData.(constructor)
    summary: <span data-ttu-id="edf18-103">Construit une nouvelle instance de la `TableData` classe.</span><span class="sxs-lookup"><span data-stu-id="edf18-103">Constructs a new instance of the `TableData` class</span></span>
    name: '(constructor)(rows, headers)'
    fullName: '(constructor)(rows, headers)'
    langs:
      - typeScript
    type: constructor
    syntax:
      content: 'constructor(rows: any[][], headers: any[]);'
      parameters:
        - id: rows
          description: ''
          type:
            - 'any[][]'
        - id: headers
          description: ''
          type:
            - 'any[]'
  - uid: office.Office.TableData.(constructor)_1
    summary: <span data-ttu-id="edf18-104">Construit une nouvelle instance de la `TableData` classe.</span><span class="sxs-lookup"><span data-stu-id="edf18-104">Constructs a new instance of the `TableData` class</span></span>
    name: (constructor)()
    fullName: (constructor)()
    langs:
      - typeScript
    type: constructor
    syntax:
      content: constructor();
  - uid: office.Office.TableData.headers
    summary: <span data-ttu-id="edf18-105">Obtient ou définit les en-têtes du tableau.</span><span class="sxs-lookup"><span data-stu-id="edf18-105">Gets or sets the headers of the table.</span></span>
    remarks: |-
      <span data-ttu-id="edf18-106">Pour spécifier des en-têtes, vous devez spécifier un tableau de tableaux qui correspond à la structure du tableau.</span><span class="sxs-lookup"><span data-stu-id="edf18-106">To specify headers, you must specify an array of arrays that corresponds to the structure of the table.</span></span> <span data-ttu-id="edf18-107">Par exemple, pour spécifier des en-têtes pour une table à deux colonnes, vous devez définir la \[ \[propriété d'en-tête sur «\]\]Header1», «header2».</span><span class="sxs-lookup"><span data-stu-id="edf18-107">For example, to specify headers for a two-column table you would set the header property to \[\['header1', 'header2'\]\].</span></span>

      <span data-ttu-id="edf18-108">Si vous spécifiez la valeur null pour la propriété headers (ou si vous laissez la propriété vide quand vous construisez un objet TableData), les résultats suivants se produisent lors de l'exécution de votre code:</span><span class="sxs-lookup"><span data-stu-id="edf18-108">If you specify null for the headers property (or leaving the property empty when you construct a TableData object), the following results occur when your code executes:</span></span>

      - <span data-ttu-id="edf18-109">Si vous insérez un nouveau tableau, les en-têtes de colonnes par défaut du tableau sont créés.</span><span class="sxs-lookup"><span data-stu-id="edf18-109">If you insert a new table, the default column headers for the table are created.</span></span>

      - <span data-ttu-id="edf18-110">Si vous remplacez ou mettez à jour un tableau existant, les en-têtes existants ne sont pas modifiés.</span><span class="sxs-lookup"><span data-stu-id="edf18-110">If you overwrite or update an existing table, the existing headers are not altered.</span></span>

      #### <a name="examples"></a><span data-ttu-id="edf18-111">Exemples</span><span class="sxs-lookup"><span data-stu-id="edf18-111">Examples</span></span>

      ```javascript
      // The following example creates a single-column table with a header and three rows.
      function createTableData() {
          var tableData = new Office.TableData();
          tableData.headers = [['header1']];
          tableData.rows = [['row1'], ['row2'], ['row3']];
          return tableData;
      }
      ```
    name: headers
    fullName: headers
    langs:
      - typeScript
    type: property
    syntax:
      content: 'headers: any[];'
      return:
        type:
          - 'any[]'
  - uid: office.Office.TableData.rows
    summary: <span data-ttu-id="edf18-112">Obtient ou définit les lignes dans le tableau.</span><span class="sxs-lookup"><span data-stu-id="edf18-112">Gets or sets the rows in the table.</span></span> <span data-ttu-id="edf18-113">Renvoie un tableau de tableaux qui contient les données du tableau.</span><span class="sxs-lookup"><span data-stu-id="edf18-113">Returns an array of arrays that contains the data in the table.</span></span> <span data-ttu-id="edf18-114">Renvoie un tableau vide s'il n'y a pas de ligne.</span><span class="sxs-lookup"><span data-stu-id="edf18-114">Returns an empty array if there are no rows.</span></span>
    remarks: |-
      <span data-ttu-id="edf18-115">Pour spécifier des lignes, vous devez spécifier un tableau de tableaux qui correspond à la structure du tableau.</span><span class="sxs-lookup"><span data-stu-id="edf18-115">To specify rows, you must specify an array of arrays that corresponds to the structure of the table.</span></span> <span data-ttu-id="edf18-116">Par exemple, pour spécifier deux lignes de valeurs de chaîne dans une table à deux colonnes, vous devez définir la propriété \[ \[Rows sur «a», «\]b \[», «c»,\]\]«d».</span><span class="sxs-lookup"><span data-stu-id="edf18-116">For example, to specify two rows of string values in a two-column table you would set the rows property to \[\['a', 'b'\], \['c', 'd'\]\].</span></span>

      <span data-ttu-id="edf18-117">Si vous spécifiez la valeur null pour la propriété Rows (ou si vous laissez la propriété vide quand vous construisez un objet TableData), les résultats suivants se produisent lors de l'exécution de votre code:</span><span class="sxs-lookup"><span data-stu-id="edf18-117">If you specify null for the rows property (or leave the property empty when you construct a TableData object), the following results occur when your code executes:</span></span>

      - <span data-ttu-id="edf18-118">Si vous insérez un nouveau tableau, une ligne vide est insérée.</span><span class="sxs-lookup"><span data-stu-id="edf18-118">If you insert a new table, a blank row will be inserted.</span></span>

      - <span data-ttu-id="edf18-119">Si vous remplacez ou mettez à jour un tableau existant, les lignes existantes ne sont pas modifiées.</span><span class="sxs-lookup"><span data-stu-id="edf18-119">If you overwrite or update an existing table, the existing rows are not altered.</span></span>

      #### <a name="examples"></a><span data-ttu-id="edf18-120">範例</span><span class="sxs-lookup"><span data-stu-id="edf18-120">Examples</span></span>

      ```javascript
      // The following example creates a single-column table with a header and three rows.
      function createTableData() {
          var tableData = new Office.TableData();
          tableData.headers = [['header1']];
          tableData.rows = [['row1'], ['row2'], ['row3']];
          return tableData;
      }
      ```
    name: rows
    fullName: rows
    langs:
      - typeScript
    type: property
    syntax:
      content: 'rows: any[][];'
      return:
        type:
          - 'any[][]'